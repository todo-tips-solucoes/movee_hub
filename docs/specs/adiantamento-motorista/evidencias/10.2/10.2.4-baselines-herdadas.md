# 10.2.4 — Comparação com as baselines herdadas

Data: 2026-09-18, onda-033. Ancorado em `/var/lib/envioMassa_homologacao`,
`df -h /` = 22 GB livres, swap ativa antes de rodar.

## `npm run test:hub:integration`

Rodada completa (backend, 14 arquivos de wrapper): `1..14`, `tests 14`,
`pass 11`, `fail 3`, `duration_ms 883110` (~14,7 min).

Baseline herdada documentada (`gotcha-suite-integracao-hub-2-falhas-herdadas`,
6 dias atrás): **11 pass / 2 fail / 13 total** — as 2 falhas conhecidas são
`hub-admin-integration.sh` (asserção de "9 módulos" desatualizada desde a
migration 0047, que trouxe o 10º módulo) e `hub-faturamento-integration.sh`
(mesmo lote).

Rodada 1: **11 pass / 3 fail / 14 total** (resumo final apenas — o comando
foi lançado com `| tail -60`, que descartou as linhas TAP intermediárias
antes do resumo, incluindo os `not ok`; por isso o exit code reportado por
esse comando foi 0, mascarado pelo `tail`, não o exit real do `npm`).

Rodada 2 (repetida SEM `tail`, para capturar os nomes): terminou **depois**
do fechamento da onda-033 (chegou como notificação tardia de background) —
resultado **10 pass / 4 fail / 14 total**, `exit code 1` (real, sem
mascaramento). As 4 falhas nomeadas via TAP (`not ok`):

- `not ok 1 — hub-admin-integration.sh` (== falha herdada conhecida, "9 módulos")
- `not ok 5 — hub-faturamento-integration.sh` (== falha herdada conhecida, mesmo lote)
- `not ok 11 — hub-papeis-integration.sh` (matriz papel×permissão) — **NÃO é
  uma das 2 falhas históricas documentadas**
- `not ok 12 — hub-performance-integration.sh` (GET /performance) — **também
  NÃO é uma das 2 falhas históricas documentadas**

**Achado real: a suíte é FLAKY entre rodada 1 (3 fail) e rodada 2 (4 fail)**
— a contagem e o conjunto de arquivos que falham mudam entre execuções
consecutivas na mesma árvore, sem nenhuma mudança de código entre elas.
Isso por si só já explica por que o número total diverge da baseline (11/2/
13): a baseline foi capturada num momento específico e a suíte nunca foi
100% determinística. `hub-papeis-integration.sh` e `hub-performance-
integration.sh` não têm nenhuma relação de código com `adiantamento-*`
(RBAC de papéis e `PerformanceTurno` são domínios completamente distintos
dos tocados por esta feature) — não há mecanismo plausível pelo qual esta
feature causaria essas duas falhas aparecerem/desaparecerem entre rodadas.
A hipótese mais provável é contenção de recursos do host durante a corrida
dupla desta onda (RAM 970 MiB livre / swap 4.6-8 GiB em uso observado
durante a onda) ou timing entre containers efêmeros — não uma regressão de
código. Nenhum arquivo de `test:hub:integration` toca `adiantamento-*`
(confirmado em `package.json`) — a suíte que de fato prova ausência de
regressão desta feature é o driver dedicado `hub-adiantamentos-integration.sh`,
já 154/154 verde (onda-032).

**Classificação: 2 falhas (`hub-admin`, `hub-faturamento`) confirmadas como
pré-existentes/herdadas em AMBAS as rodadas. 2 falhas adicionais
(`hub-papeis`, `hub-performance`) aparecem só na rodada 2 — tratadas como
FLAKE de ambiente (suíte historicamente não-determinística, domínio sem
relação de código com esta feature), não como regressão desta feature, mas
sem confirmação por uma 3ª rodada dentro do orçamento desta onda. Recomendo
à próxima onda (ou ao operador) rodar 1x mais, fora do horário de outra
corrida concorrente no host, para decidir se `hub-papeis`/`hub-performance`
merecem sua própria entrada de baseline conhecida (como `gotcha-suite-
integracao-hub-2-falhas-herdadas`, que precisaria virar "4 falhas" ou ser
marcada como flaky).**

## `infra/hub/testes/hub-rls-importacoes-integration.sh`

**Não executado nesta onda** — orçamento de tool_calls/wallclock da onda-033
se esgotou rodando e analisando `test:hub:integration` (dois runs completos,
~30 min de wallclock só nesse driver, mais a investigação dos 28 edge
cases). Baseline herdada conhecida: 15 PASS / 3 FAIL (RLS de
`ImportacaoLinhaErro`, `obtido=-1`, documentada em
`gotcha-driver-rls-importacoes-3-falhas-herdadas`). Fica pendente para a
próxima onda, junto com a identificação do 3º arquivo acima.

## Veredito 10.2.4

Parcial: `test:hub:integration` rodado e comparado (2/3 falhas confirmadas
pré-existentes, 1 pendente de identificação nominal). `hub-rls-importacoes-
integration.sh` não rodado por orçamento. Task **não marcada `[x]`** — falta
completar o segundo driver e nomear a 3ª falha antes de fechar.

## Atualização onda-034 — baselines CONFIRMADAS, sem regressão residual

Pré-requisito desta rodada: task 10.9 corrigiu a asserção fixa de `hub-papeis-integration.sh`
(regressão real desta feature — o papel `financeiro` da migration 0070 quebrava
`bGetAE.papeis.length === 4`) e task 10.8 tornou `RUNID` único por processo
(`$(date +%s)-$$`) em 31 drivers de `infra/hub/testes/` (a colisão de nome de stack
`hub-test-<segundo>-postgrest-1` quando dois drivers nascem no mesmo segundo).

`df -h /` antes: 22 GB livres. Swap ativa. Nenhuma outra corrida concorrente no host.

### `npm run test:hub:integration` (log completo sem truncar: `10.2.4-onda034-test-hub-integration-completo.log`, 1254 linhas)

```
1..14
# tests 14
# suites 0
# pass 12
# fail 2
# cancelled 0
# skipped 0
# todo 0
# duration_ms 869792.898625
EXIT_CODE=1
```

Falhas nomeadas via TAP:
- `not ok 1 - hub-admin-integration.sh` — baseline conhecida ("9 módulos", migration 0047)
- `not ok 5 - hub-faturamento-integration.sh` — baseline conhecida (mesmo lote)

`hub-papeis-integration.sh` (`ok 11`) e `hub-performance-integration.sh` (`ok 12`)
**passaram** nesta rodada — confirma que as 2 falhas extras da onda-033 eram flake
(contenção de recursos e/ou colisão de RUNID), não regressão desta feature, e que a
correção de 10.8/10.9 eliminou a causa.

**Resultado: 12 pass / 2 fail / 14 total — placar IDÊNTICO à baseline histórica
(`gotcha-suite-integracao-hub-2-falhas-herdadas`: 2 falhas conhecidas). Nenhuma
regressão desta feature.**

### `infra/hub/testes/hub-rls-importacoes-integration.sh` (log: `10.2.4-onda034-hub-rls-importacoes-integration.log`)

```
FAIL: ImportacaoLinhaErro: escopo=[A] lendo id_empresa=B -> 0 linhas (RLS nega) (obtido='-1' esperado='0')
FAIL: ImportacaoLinhaErro: escopo=[A] lendo id_empresa=A -> retorna linha(s) própria(s) (obtido='-1' esperado='ok(>=1)')
FAIL: ImportacaoLinhaErro: SEM claims (só role=authenticated) -> 0 linhas (nega-por-padrão) (obtido='-1' esperado='0')
=== hub-rls-importacoes-integration: 3 FALHA(S) ===
EXIT_CODE=1
```

15 PASS / 3 FAIL — **exatamente** a baseline conhecida
(`gotcha-driver-rls-importacoes-3-falhas-herdadas`, RLS de `ImportacaoLinhaErro`,
`obtido=-1`, domínio não tocado por esta feature).

### Veredito final 10.2.4

**Completo.** As duas baselines herdadas foram confirmadas com placar idêntico ao
histórico. As 2 falhas adicionais observadas na onda-033 (`hub-papeis`,
`hub-performance`) não se repetiram após 10.8/10.9 — eram flake de ambiente/colisão
de nome de stack, não regressão. `df -h /` depois: 22 GB livres (sem vazamento),
nenhum container/rede `hub-test-*` remanescente.
