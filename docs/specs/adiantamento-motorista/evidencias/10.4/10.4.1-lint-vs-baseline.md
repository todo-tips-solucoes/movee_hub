# 10.4.1 — Lint nos três projetos tocados, comparado com a baseline

Data: 2026-09-18, onda-033.

## backend

Sem lint configurado — `app_homologacao/backend/package.json` não tem script
`"lint"` (confirmado: `grep -n '"lint"' package.json` não retorna nada).
Nenhum número a reportar; não é regressão desta feature (nunca existiu).

## frontend_v2

`npm run lint` (eslint) na árvore atual (com todas as mudanças desta feature
não commitadas):

```
✖ 28 problems (5 errors, 23 warnings)
```

Os 5 erros:

| Arquivo | Linha | Regra |
|---|---|---|
| `app/dashboard/configuracoes/aparencia/page.tsx` | 35, 103, 107 | `@typescript-eslint/no-explicit-any` |
| `app/dashboard/configuracoes/grupo/page.tsx` | 147 | `@typescript-eslint/no-explicit-any` |
| `components/empresa-selector.tsx` | 53 | `Calling setState synchronously within an effect...` |

Os 3 arquivos acima **não fazem parte do escopo desta feature** e **não
aparecem no `git status --short`** (confirmado via
`git status --short <cada arquivo>` = saída vazia para os três) — ou seja,
são idênticos ao HEAD de `main`, portanto os 5 erros são 100%
**pré-existentes**. Não precisou de `git stash` para provar isso: arquivo
sem diff não pode ter introduzido erro novo.

**Resultado: 0 erros novos desta feature.** As 23 warnings (não bloqueiam
por definição do rito) estão majoritariamente em arquivos de teste E2E
(`tests/e2e-hub-adiantamentos/adiantamentos.spec.ts`,
`tests/e2e-hub-avisos/hub-avisos.spec.ts`, etc.) — nenhuma delas é erro.

## frontend_motorista

`npm run lint` falha antes de rodar qualquer regra:

```
Oops! Something went wrong! :(
ESLint: 9.39.4
ESLint couldn't find an eslint.config.(js|mjs|cjs) file.
```

Confirmado: não existe `eslint.config.js` nem `.eslintrc*` no projeto, e
não há histórico de git para nenhum desses nomes de arquivo
(`git log --oneline -- eslint.config.js .eslintrc.json` vazio) — o projeto
**nunca teve lint configurado**. Não é regressão desta feature; nenhum
número a reportar (rodar `eslint` sem config não produz uma contagem de
erros válida, só falha de setup).

## Veredito 10.4.1

Nenhum erro novo bloqueante introduzido por esta feature nos três
projetos. `backend` e `frontend_motorista` não têm lint configurado
(gaps pré-existentes, fora do escopo corrigir aqui). `frontend_v2` tem 5
erros pré-existentes em arquivos não tocados por esta feature — 0 novos.
